name: Docker Test
on: push

jobs:
  test-docker:
    runs-on: ubuntu-latest
    container:
      image: node:23-alpine3.19
      env:
        NODE_ENV: development
      ports:
        - 80
    steps:
      - name: Display node version
        run: |
          node -v
          cat /etc/os-release