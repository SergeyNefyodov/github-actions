name: Third Workflow
on: [push]

jobs: 
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Command in MultiLine
        run: |
          pwd
          ls -a
          ls --help
  