name: First Workflow
on: [push]

jobs: 
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display some text
        run: echo "Hello world!"
      - name: Command in MultiLine
        run: |
          node -v
          npm -v
  